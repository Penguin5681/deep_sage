import 'package:flutter/material.dart';

class VisualizeTab extends StatelessWidget {
  const VisualizeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: const Text('Actual charts here'));
  }
}
